import 'dart:io';

void main() {
    int X = int.parse(stdin.readLineSync());
    double Y = double.parse(stdin.readLineSync());
    
    double Z = X / Y;
    
    print('${Z.toStringAsFixed(3)} km/l');
}
